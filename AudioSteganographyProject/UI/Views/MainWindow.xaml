<Window x:Class="AudioSteganographyProject.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioSteganographyProject"
        mc:Ignorable="d"
        Title="Steganography Project" Height="600" Width="1000"
        Background="Beige">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="800" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5,5,5,5" Background="Gray">
            <Button Content="About" Margin="0,5,0,5" Click="onAboutButton_Click" />
            <Button Content="Create New Song" Margin="0,5,0,5" Click="onHideSongButton_Click"/>
            <Button Content="Read Hidden Info" Margin="0,5,0,5" Click="onReadHiddenInfoButton_Click"/>
        </StackPanel>
        <Frame x:Name="MainDisplayFrame" Margin="10,10,10,10" Grid.Column="1" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
